.header {
   position: absolute;
   width: 100%;
   color: $text-color;
   z-index: 1;

   &__panel {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-top: 50px;
   }
}
