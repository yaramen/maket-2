.menu {
    &__list {
        @include list-reset();
        display: flex;
    }

    &__link {
        display: block;
        padding: 10px 30px;
    }

    &.-footer {
        position: relative;
        top: -10px;
    }
}
